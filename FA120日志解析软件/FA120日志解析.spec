# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['fa120_log_parser.py'],
    pathex=[],
    binaries=[],
    datas=[('standard_sequences.json', '.'), ('theory_data.json', '.'), ('motor_aliases.json', '.'), ('param_action_names.json', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='FA120日志解析',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
