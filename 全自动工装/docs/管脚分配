### 项目目的：
使用STM32C8T6 设计开发一个用于生产的工装工具
### 使用任意：
生产调试人员
### 项目概述：
使用STM32 开发一个生产调试用的工装工具，具体包括
2路步进电机驱动 驱动器为TMC2226（每路包括使能管脚、方向管脚、和PWM波输出 3个管脚）并使用一路UART同时连接2个驱动器进行指令控制
2路开关输出（用于测试泵阀等），1路使用GPO控制MOS管开启，另外一路物理按键开启（无需管脚控制）
2两路GPI外部输入（用于测试光开关，优先接外部中断）
1路EEPROM   AT24C32  IIC接口用于掉电保存参数（仅一些电机的转速和电流参数，是否有必要，还是可以用STM32内部的存储） 
1路OLED接口，IIC接口
1路条码模块接口：串口+一个触发IO管脚
1路热敏电阻采集电路，使用一个内部ADC接口
1路OPB9000 光开关的接口，使用2路普通IO进行模拟通讯
1路旋转编码器（带按键）用于电机参数的设置，旋转编码器AB相输入STM32，需要STM32的脉冲捕获模式 按键信号也输入STM32
（2个步进电机驱动器，需要分别设置，实现上，是否仅通过一个带按钮的编码器就，通过按钮选择来实现，还是需要2个旋钮
功能包括：选择驱动器1还是2，对选中的驱动器，通过旋钮，来修改转速，修改后保存）
1路UART接口，用于热敏打印机（如管脚不够用，该功能不用设计，我查看文档发现，如果EEPROM不用外部的，就可以多一路UART）
2路外部按键，用于功能或者模式选择预留
2路LED灯预留

### 0529补充
1、2路旋转编码器（带按键）用于电机参数的设置和功能选择，旋转编码器AB相输入STM32，需要STM32的脉冲捕获模式 按键信号也输入STM32。  所以一共有4个脉冲捕获器和2个按键输入
2、去掉AT24C32 使用STM32 内部FLASH存储
3、2路电机，每路都增加2个按键输入，用于正转或者反转，总共4个按键
4、再一个按键，用于按下，控制DO1输出
5、LED灯就预留一路就可以
6、条码模块，不用按键触发了 直接通过串口指令触发就可以

### 0530补充

1、调整以下管脚 去掉第二路旋转编码器，仅用1路编码器实现2路的功能
2、故把PB6和PB7 改成IIC2，  PA11和PB8 改成2个按键输入，
3、PB2 boot1接地  光开关输入2接入到PB9