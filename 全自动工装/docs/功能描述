### 主界面
左侧显示M1和M2电机的转速和电流
右侧显示打印、校准、扫码
中间顶部显示温度
中间底部显示光开关1和光开关2的触发状态
在这个界面下：温度每秒刷新一一次，
光开关1和光开关2，如果触发，产生外部中断，进行刷新，下降沿触发 显示打勾 上升沿触发，显示打X
### 旋钮编码器功能说明

通过AB相位触发时间前后，来表示旋钮是正转还是反转
旋转用于在不同的功能中进行选择，主界面可以选择的功能包括
M1、M2、打印、校准、扫码  旋转后在这个5个功能框体中进行选择
按下旋钮的按键后，表示选中。 长按旋钮的按钮（2秒），表示保存设置并返回（M1和M2用）。

### M1和M2的功能说明

选中M1或M2后，按下，进入设置选择，包括转速和电流
旋钮在转速和电流中进行选择，再次按下，进入转速或电流设置
转速设置：旋钮正转，增加 每触发一次编码器，增加25RPM，最小转速25RPM 最大转速1500RPM
反转，降低25RPM.
电流设置：旋钮正转，增加 每触发一次编码器，增加Irun增加1 ，最小电流的时候，Irun=0 最大电流 1.35A。 显示的电流值为Irun换算之前后的。
反转，降低Irun

转速和STEP PWM输出对应关系：TMC2226硬件8细分，输出8个脉冲表示1步
电机步距角1.8度，故PWM输出8个脉冲，表示电机运动1.8度。1600个脉冲，电机转1圈
根据这个关系，计算RPM。 每分钟转速和PWM频率关系

电流设置，通过UART进行电流设置，TMC2226 外接电阻150mR  Irun=31 表示1.35A
按照这个关系换算电流   电流值=1.35*(Irun+1)/32

外部按键4个 PA4按下 用于控制M1电机正转 DIR=1 
           PA12按下 用于控制M1电机反转 DIR=0
           PA15按下 用于控制M2电机正转 
           PB3按下 用于控制M2电机反转
电机在没有按下以上4个按钮的时候，均出于禁能状态（EN1和EN2拉高）
当按下4个外部按键的时候，先把对应的EN1或者EN2 拉低，再根据按键，确定是正转还是反转，控制DIR1或者0，再进行PWM输出，控制电机旋转。松开按键，电机停止，并继续拉高EN禁能电机

### 打印

选中打印后，通过串口(波特率9600），输出5行打印内容，内容如下
A
BB
CCC
DDDD
EEEEE

### 扫码

按下扫码后，通过串口（波特率9600），发送扫码指令给外部条码模块，模块扫码后， 返回信息，并把返回的条码内容显示在屏幕上

指令及返回，查看MZR813H用户手册
开始扫码指令：02 00 01 05 00 04 11 00 00 01 10 03
并把串口返回的数据，全部显示。
如3秒内无返回内容，则显示扫码失败。

### 校准
查看OPB9000程序

第一次按下校准：STM32的工作流程和程序里面接收后
## / CS / 0 / 0 / 0 / ##  指令一样，
只是把校准成功或校准失败的返回内容改一下
校准成功后，原OPB9000工程返回
1、收到## / CS / 0 / 0 / 0 / ##
2、写灵敏度15 极性反向
3、写灵敏度成功
4、写灵敏度4，极性正向
5、移开样本管，蓝色指示灯灭掉后，发送读取指令RR
校准失败，原OPB9000工程返回
1、收到## / CS / 0 / 0 / 0 / ##
2、写灵敏度15 极性反向
3、写灵敏度15 极性正选
4、写灵敏度成功
5、校准失败，检查试剂位置是否过远
4、写灵敏度4，极性正向

现改为：
校准成功：成功，移开管后，再次按下按键（见image 按下校准后.png)
校准失败：失败，请检查试管距离

当校准成功后，提示再次按下按键 这个时候，继续发送指令
## / RR / 0 / 0 / 0 / ##
如读取成功（说明写入和读取的一致）
原OPB9000工程返回
ReadRequest
Bank1 CA:1 AGC：2 LED833
   Bank2 REF:2 DS:00 OP:1
   Bank3 SB:0 EF:0
   读取成功 灵敏度为2，极性正向。
如读取失败，返回(说明写入和读取的不匹配)
readRequest
Bank1 CA:1 AGC：2 LED833
   Bank2 REF:2 DS:00 OP:1
   Bank3 SB:0 EF:0
   读取失败，请移开样本，使蓝灯熄灭后再读。
现改为：
读取成功：读取成功  AGC：XX  LED:XXX(显示AGC和LED的值)
读取失败：读取失败，移开样本后再次读取（此时可以再次按下旋钮，读取）   


### DO功能
按下DO_KEY PC13
GPO1（PB0）拉高，控制DO输出

### OLED显示

使用1.54寸  分辨率128*64的OLED显示屏
阅读OLED规格书，通过IIC指令，控制OLED进行显示。

### 温度传感器

先实现ADC采集到的值的显示，具体的换算公式，后续补。
